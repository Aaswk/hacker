# 06 — 团队分工与技术栈
---

## 1. 团队分工：三层 Owner

三个人不按「产品 / 前端 / 后端 / AI」平均切，而按 **可独立开发、可独立提交、低冲突合并** 的边界拆成三层：

```
A：体验层  Product / Frontend / Demo
        │
        │ API Contract
        ↓
B：系统层  Backend / Data / Integration
        │
        │ AI Service Interface
        ↓
C：智能层  AI / Vision / Intelligence
```

| 角色 | Owner | 负责范围 | 交付物 |
|---|---|---|---|
| **A** | 体验层 | 产品定义、用户路径、页面结构、摄像头调用、宠物动画、结果可视化、Demo 流程、答辩演示 | 评委看到的一切 |
| **B** | 系统层 | API、数据校验、状态管理、数据库、部署、日志、系统集成 | 项目的“中枢” |
| **C** | 智能层 | 视觉理解、行为识别、人格化叙事、结构化输出、效果评测 | 项目的“智能” |

### Demo 主链路与负责人对应

```
Camera
  ↓
Frontend Capture          A
  ↓
POST /api/analyze
  ↓
Backend Orchestrator      B
  ↓
Vision Service            C
  ↓
Structured JSON
  ↓
State / Database          B
  ↓
Frontend Visualization    A
```

**核心原则：按代码所有权分工，而不是按“功能想法”分工。**

---

## 2. 技术栈总览

| 层 | 技术 | 用途一句话 |
|---|---|---|
| 前端 | Next.js + React + TypeScript | 页面与宠物动画的渲染载体 |
| 样式 | Tailwind CSS | 快速实现 Landing / 主界面视觉 |
| 摄像头 | Browser MediaDevices API (`getUserMedia`) | 让宠物拥有“眼睛” |
| 前端状态 | Zustand | 管理宠物状态与观察日志 |
| 动画 | Framer Motion | 宠物情绪 / 动作反馈 |
| 后端 | Python + FastAPI | 编排整条链路 |
| 数据校验 | Pydantic | 保证 AI 输出可靠可用 |
| 数据库 | PostgreSQL + SQLAlchemy | 存观察日志与档案 |
| 服务化 | Docker + uvicorn | 一键起服务、稳定演示 |
| 视觉 | OpenCV + Pillow | 画面预处理 |
| 视觉理解 | VLM / Vision Model | 判断“发生了什么” |
| 叙事生成 | LLM | 生成外星研究员的荒诞解释 |
| 测试 | pytest | 保证链路稳定 |

---

## 3. 每个技术栈用在了哪个功能上

### 3.1 体验层（A）

| 技术 | 用在哪 | 具体功能 |
|---|---|---|
| Next.js | Landing 页、宠物主界面 | 「开始体验」入口、页面路由 |
| React | 三个核心界面状态 | 初始状态 / 感知状态 / Aha 反馈状态 |
| TypeScript | 全前端 | 与后端 Contract 对齐的类型约束 |
| Tailwind CSS | 全局样式 | 深色科幻风界面、字幕与日志面板 |
| MediaDevices API | 摄像头取流 | 「开启摄像头」后宠物进入观察模式 |
| Zustand | 前端状态 | 宠物状态机、观察日志列表、物种卡数据 |
| Framer Motion | 宠物动画 | 眨眼 / 东张西望 / 抬头 / 警戒 / 兴奋 |
| fetch | 与后端通信 | 截帧上传、拉取分析结果 |
| API Contract | 前后端约定 | `analyze` 请求与响应字段 |

### 3.2 系统层（B）

| 技术 | 用在哪 | 具体功能 |
|---|---|---|
| FastAPI | 后端入口 | `POST /api/analyze`、会话与日志接口 |
| Pydantic | 请求 / 响应校验 | AI 返回结果必须通过 Schema 才算有效 |
| 事件状态机 | 行为判定 | 避免同一动作重复触发，输出离散事件 |
| PostgreSQL | 数据持久化 | 观察日志、行为计数、物种卡 |
| SQLAlchemy | 数据访问 | 事件写入与统计查询 |
| Docker | 环境一致性 | 演示机与开发机行为一致 |
| uvicorn | 服务运行 | 本地启动后端 |
| 日志 | 运行监控 | 记录事件流与异常，便于现场排查 |
| 部署 | 演示环境 | 保证 3 分钟内 Demo 稳定运行 |
| pytest | 接口测试 | 校验契约与异常分支 |

### 3.3 智能层（C）

| 技术 | 用在哪 | 具体功能 |
|---|---|---|
| Python | 智能层语言 | 后端 / AI 同语言，集成成本最低 |
| OpenCV | 画面预处理 | 降采样、节流，降低推理开销 |
| Pillow | 图像处理 | 编码与格式统一 |
| Vision / VLM | 视觉理解 | 判断「有人出现 / 靠近 / 喝水 / 伸展 / 离开」 |
| 结构化输出 | 结果规范 | 输出 `person_detected / action / confidence` |
| LLM + Prompt | 叙事生成 | 「该生物正在为内部海洋补充液体」 |
| 状态映射 | 事件 → 宠物状态 | `approaching → excited` 等规则映射 |
| 评测集 | 效果验证 | 正常、无人、多人、暗光、模糊等场景 |

---

## 4. 仓库结构与协作方式

### 4.1 Monorepo

三人共用**一个仓库、按目录分所有权**，避免最后复制粘贴合并：

```
project/
├── frontend/      # A
├── backend/       # B
├── ai/            # C
├── shared/
│   └── contracts/ # A + B + C 共同维护
├── docs/
├── docker-compose.yml
└── README.md
```

`shared/contracts/` 不属于任何一个人，是三层之间的“外交条约”——任何接口字段的改动都必须三人共同确认。

### 4.2 边界约定

| 模块 | Owner | 可修改 | 不修改 |
|---|---|---|---|
| Frontend | A | `frontend/**` | `backend/**`、`ai/**` |
| Backend / DB | B | `backend/**`、schema | `frontend/**`、`ai/**` |
| AI | C | `ai/**` | 前后端业务代码 |
| Contract | A+B+C | `shared/contracts/**` | 必须共同确认 |
| Deploy | B | deployment | — |
| Demo | A | demo 配置 | — |
| Eval | C | AI tests | — |

### 4.3 分支与提交

分支按**任务**建，不按人建：

```
main ← feature/camera-capture
     ← feature/analyze-api
     ← feature/vision-detection
     ← fix/camera-permission
```

提交使用 Conventional Commits + Scope，让 Commit History 直接可读：

```
feat(frontend): add camera capture component      → A
feat(api): add image analysis endpoint            → B
feat(ai): add approaching action detection        → C
feat(contract): define analysis response schema   → 共同
```

原则：**小功能、小 Commit、小 PR、快速合并 main。**

---

## 5. 开发节奏：Mock 优先打通链路

第一轮目标不是各自写完完整功能，而是**先用假 AI 打通全链路**：

```
A：摄像头 → 得到一张图片
B：POST /api/analyze → 接受图片 → 返回 Mock JSON
C：analyze(image) → 返回 Mock 结果
```

统一约定结果格式：

```json
{
  "status": "success",
  "detected": true,
  "confidence": 0.93,
  "action": "approaching",
  "petState": "excited",
  "message": "Someone is approaching!"
}
```

第一轮结束即可跑通：

```
摄像头 → 截帧 → Frontend → Backend → AI Mock → Backend → Frontend → 宠物产生反应
```

第二轮 C 把 Mock 换成真实视觉模型，A 继续做动画，B 继续做数据库，三人真正并行：

```
             ┌→ A：UI / Animation
主链路打通 ──┼→ B：DB / API / Deploy
             └→ C：Vision / AI / Eval
```

---

## 6. 一句话协作模型

> **三人分别负责「体验层（Frontend）—系统层（Backend）—智能层（AI）」，以 `shared/contracts` 作为模块边界，以 Mock 优先打通 Camera → Frontend → Backend → AI → State → Visualization 的完整 Demo 链路；所有功能采用短生命周期 Feature Branch + 小粒度 PR 合并，确保三人并行开发、持续集成，并在黑客松后期低冲突完成合并。**
